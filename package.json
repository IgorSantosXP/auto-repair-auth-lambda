{
  "name": "auto-repair-auth-lambda",
  "version": "1.0.0",
  "description": "Serverless CPF authentication for the auto repair shop API",
  "type": "module",
  "main": "src/index.mjs",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "build": "rm -rf dist && mkdir -p dist && cp -r src package.json package-lock.json dist/ && npm ci --omit=dev --prefix dist"
  },
  "dependencies": {
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.13.1"
  }
}
